{
  "name": "yc-fe-lib",
  "version": "1.0.0",
  "description": "Collection libraries",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rm -rf build",
    "pretest": "npm run clean",
    "test": "jest --config jestconfig.json --coverage",
    "build": "npm run test && tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "precommit": "npm run format"
  },
  "keywords": [],
  "author": "Yuda Cogati",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.20",
    "lodash-inflection": "^1.5.0",
    "moment": "^2.29.1",
    "moment-timezone": "^0.5.31",
    "titleize": "^2.1.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "jest": "^26.6.0",
    "prettier": "^2.0.5",
    "ts-jest": "^26.4.1",
    "tslint": "^6.1.2",
    "tslint-config-prettier": "^1.18.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  },
  "files": [
    "build/**/*"
  ]
}
